# 📖 دليل تشغيل «الناشر الأوتوماتيكي» لوصية — من الألف للياء

> **الفكرة في 30 ثانية:** الأداة دي ملفات صغيرة بتترفع على GitHub، و**GitHub Actions** (خدمة مجانية مدمجة في GitHub) بيشغّلها تلقائيًا 3 مرات يوميًا. كل مرة: بتولّد بوست جديد (من مكتبة 46 بوست جاهز بدون تكرار) + بطاقة صورة بنفس هوية الموقع، وتنشرها على كل منصة ربطتها. **التكلفة الإجمالية: 0 جنيه — للأبد.**

```
GitHub Actions (مجدول: صبح / ظهر / مغرب)
        │
        ▼
   run.mjs يولّد البوست + البطاقة
        │
        ├── تليجرام (قناتك) ── الأسهل، ابدأ بيه
        ├── X / تويتر (حسابك)
        ├── فيسبوك (صفحتك)
        └── انستجرام (حسابك) ── مع الصورة
        │
        ▼
   post-log.md ── سجل بكل بوست اتنشر (بتشوفه على GitHub مباشرة)
```

---

## المحتويات
1. [رفع الأداة على GitHub](#1-رفع-الأداة-على-github)
2. [أول تشغيل تجريبي](#2-أول-تشغيل-تجريبي)
3. [ربط تليجرام — ابدأ من هنا (15 دقيقة)](#3-ربط-تليجرام)
4. [ربط X / تويتر (30 دقيقة)](#4-ربط-x--تويتر)
5. [ربط فيسبوك + انستجرام (45 دقيقة)](#5-ربط-فيسبوك--انستجرام)
6. [تغيير مواعيد النشر](#6-تغيير-مواعيد-النشر)
7. [إضافة وتعديل البوستات](#7-إضافة-وتعديل-البوستات)
8. [نصائح أمان الحسابات الجديدة](#8-نصائح-أمان-الحسابات-الجديدة)
9. [حل المشاكل](#9-حل-المشاكل)
10. [التكلفة وليه مفيش تيك توك](#10-التكلفة-وليه-مفيش-تيك-توك)

---

## 1) رفع الأداة على GitHub

1. افتح **github.com** → اضغط زرار **+** فوق يمين → **New repository**
2. الاسم: `wasiya-social` — اختار **Public** (مهم: الريبو العام معاه GitHub Actions ببلاش وبلا حدود، ومفيش أي أسرار جوه الملفات — الأسرار بتتحط في مكان تاني آمن) → **Create repository**
3. على صفحة الريبو الجديد: زرار **Add file → Upload files** → اسحب الملفات دي من المجلد اللي معك:
   - `run.mjs`
   - `package.json`
   - `README.md`
   - `SETUP.md`
   - `.gitignore`
   - → اضغط **Commit changes**
4. دلوقتي ملف الجدولة (لازم يتعمل بطريقة خاصة لأنه في مجلد مخفي):
   - زرار **Add file → Create new file**
   - في خانة الاسم اكتب المسار كامل (GitHub هيعمل المجلدات لوحده):
     `.github/workflows/post.yml`
   - افتح ملف `post.yml` اللي معك بالـ Notepad → انسخ محتواه كله → الصقه
   - **Commit changes**

✅ خلصت الرفع. من دلوقتي GitHub واخد الأداة تحت جناحه.

---

## 2) أول تشغيل تجريبي

(دي بتحصل بدون أي حسابات — عشان تتأكد إن كل حاجة شغالة)

1. في الريبو: افتح تبويب **Actions** فوق
2. لو ظهرلك زرار أخضر مكتوب فيه "I understand my workflows, go ahead and enable them" → اضغطه (بيظهر أول مرة بس)
3. من القائمة الشمال: اختار **ناشر السوشيال**
4. اضغط **Run workflow** → سيب "وضع التجربة" **مفعّل** (✓) → اضغط زرار **Run workflow** الأخضر
5. استنى دقيقة → هيظهر سطر أصفر بيتحول أخضر ✅ → اضغط عليه → **Run post** → شوف اللوج:
   - هيقولك: البوست رقم كام، عنوانه، "بطاقة الصورة جاهزة"، ومعاينة النصوص
   - عشان تشوف البطاقة نفسها: افتح آخر run → من اللوج نزّل artifact؟ لأ الأسهل: شغل نفس الأمر على جهازك لو عندك Node، أو اعتمد على عينات الصور اللي معايا في `card-samples`

> **وضع التجربة** = يولّد كل حاجة ويعرضها بس، من غير أي نشر. ده الافتراضي في التشغيل اليدوي عشان تجرب براحتك. الجدولة اليومية بتنشر حقيقي.

---

## 3) ربط تليجرام

> **الأسهل والأسرع — ابدا بيه وهتلاقي أول بوست عندك في القناة خلال ربع ساعة**

### أ) اعمل البوت (2 دقيقة)
1. افتح تليجرام وابحث عن **@BotFather** (الأزرق المعتمد)
2. ابعتله: `/newbot`
3. هيقولك "choose a name" → اكتب: `وصية` 
4. هيطلب username لازم ينتهي بـ `bot` → اكتب: `wasiya_app_bot` (أو أي حاجة مش مأخوذة)
5. هيبعتلك رسالة فيها **Token** زي: `123456789:AAH3x9...` → **انسخه**

### ب) اعمل القناة (2 دقيقة)
1. في تليجرام: زرار القلم ✏️ → **New Channel**
2. الاسم: `وصية — خزنة أسرارك بعد رحيلك` → نوعها **Public**
3. هيديك لينك زي `t.me/wasiya_app` → **خلي بالك من الاسم اللي بعد t.me/** (هنحتاجه)
4. ادخل إعدادات القناة → **Administrators** → **Add Admin** → ابحث عن username البوت اللي عملته → ضيفه أدمن (دي اللي بتسمحله يبعت في القناة)

### ج) حط المفاتيح في GitHub (2 دقيقة)
1. افتح ريبو `wasiya-social` → **Settings** (بتاع الريبو مش حسابك) → من الشمال: **Secrets and variables → Actions**
2. زرار **New repository secret** → اعمل اتنين:

| Name | Secret value |
|---|---|
| `TELEGRAM_BOT_TOKEN` | التوكن اللي نسخته من BotFather |
| `TELEGRAM_CHANNEL` | `@wasiya_app` (اسم قناتك اللي بعد t.me/ — لازم معاه علامة @) |

### د) اختبر فورًا
Actions → ناشر السوشيال → **Run workflow** → المرة دي **شيل علامة** "وضع التجربة" → Run → بعد دقيقة: افتح قناتك في تليجرام — **البوست + البطاقة الموجودين** 🎉

---

## 4) ربط X / تويتر

> الباقة المجانية بتتيح **~500 بوست في الشهر** — إحنا بنستهلك 90 بس (3 × 30)

### أ) جهّز الحساب (مهم جدًا — الأول)
1. افتح حساب X جديد بالإيميل اللي انت عايزه للتسويق
2. **قبل أي ربط**: ظبّط البروفايل (صورة/بيو/خلفية) وانزل **2-3 تويتات يدوي** على مدار يوم-يومين. الحسابات الفاضية اللي بتنشر أوتوماتيك فورًا بتتعمل ليها Flags

### ب) خد مفاتيح المطورين (10 دقايق)
1. افتح **developer.twitter.com** → **Sign up** بنفس الحساب
2. هيسألك عن الاستخدام → اختار/اكتب: "نشر تحديثات منتجي تلقائيًا" → اختار الباقة **Free**
3. هيطلب منك إنشاء **Project** → سميه `wasiya` → هييعمل App تلقائي
4. افتح إعدادات الـ App → **User authentication settings → Set up**:
   - App permissions: **Read and write** ← ⚠️ دي أهم خطوة
   - Type of App: **Web App / Automated App or Bot**
   - Callback/Website: اكتب `https://wasiya-taupe.vercel.app` → Save
5. روح تبويب **Keys and tokens**:
   - **API Key and Secret** (Consumer Keys) → اضغط Regenerate/View → انسخ الاتنين
   - **Access Token and Secret** → اضغط **Generate** → انسخ الاتنين
   - ⚠️ لو التوكنز اتولدت قبل ما تظبط "Read and write" هيبقوا للقراية بس — regenerate تاني بعد التظبيط

### ج) حطهم في GitHub Secrets (4 أسرار)

| Name | قيمته |
|---|---|
| `X_API_KEY` | API Key |
| `X_API_SECRET` | API Key Secret |
| `X_ACCESS_TOKEN` | Access Token |
| `X_ACCESS_SECRET` | Access Token Secret |

اختبر بنفس طريقة تليجرام (Run workflow من غير وضع التجربة) — البوست هيظهر على الحساب.

---

## 5) ربط فيسبوك + انستجرام

> الاتنين بيشتغلوا مع بعض من نفس مكان (منصة Meta). الطريق أطول شوية بس بيتعمل **مرة واحدة** — التوكن الناتج **مش بينتهي** (إلا لو غيّرت باسوورد فيسبوك)

### أ) جهّز الحسابات (10 دقايق)
1. **فيسبوك:** اعمل صفحة (Page) جديدة باسم **وصية** — من زرار Pages → Create New Page — وظبّط صورتها ووصفها
2. **انستجرام:** افتح حساب IG جديد → من الإعدادات: **Account type and tools → Switch to professional account → Business** → اربطه بالصفحة اللي عملتها في الخطوة اللي فاتت
3. سجّل دخول على **developers.facebook.com** بنفس حساب فيسبوك → أول مرة هيطلب تأكيد هاتف/معلومات بسيطة

### ب) اعمل تطبيق المطورين (5 دقايق)
1. في developers.facebook.com: **My Apps → Create App**
2. نوع الاستخدام: اختار **Business** → اسم التطبيق: `wasiya social` → Create
3. مش محتاج تضيف أي منتج — هنعمل كل حاجة من أداة الاستكشاف

### ج) ولّد التوكن الدائم (10 دقايق)
1. افتح: **developers.facebook.com/tools/explorer** (Graph API Explorer)
2. فوق يمين: اختار تطبيقك `wasiya social`
3. عند خانة الـ Token: اضغط على كلمة **"User or Page"** جنب زرار Generate → اضغط **Generate Access Token**
4. هيفتحلك قايمة صلاحيات — علّم عليها دول بالظبط:
   - `pages_show_list`
   - `pages_read_engagement`
   - `pages_manage_posts`
   - `instagram_basic`
   - `instagram_content_publish`
   → Continue → عطّلهم الصلاحيات → هيديك **توكن مؤقت (ساعة واحدة)**
5. **طوّله لعمر أطول:** افتح **developers.facebook.com/tools/debug/accesstoken** → الصق التوكن → اضغط زرار **Extend Access Token** → انسخ **التوكن الطويل الجديد** (صالح 60 يوم)
6. ارجع لـ Graph Explorer → استبدل التوكن في الخانة بالتوكن الطويل الجديد
7. في خانة الاستعلام اكتب: `me/accounts` → اضغط Submit → هيطلعلك صفحاتك مع **access_token** لكل صفحة → **انسخ توكن الصفحة** (ده المش بينتهي خلاص — هو اللي هنستخدمه) و**انسخ id الصفحة** رقمي
8. في خانة الاستعلام اكتب: `صفحتك_ID?fields=instagram_business_account` (بدّل صفحتك_ID برقم الصفحة) → Submit → هيطلعلك رقم **instagram_business_account** → انسخه (ده الـ ID بتاع انستجرام)

### د) حطهم في GitHub Secrets (3 أسرار)

| Name | قيمته |
|---|---|
| `FB_PAGE_ID` | رقم id الصفحة (من الخطوة 7) |
| `FB_PAGE_TOKEN` | توكن الصفحة الدائم (من الخطوة 7) |
| `IG_USER_ID` | رقم انستجرام (من الخطوة 8) |

⚠️ **لو غيّرت باسوورد فيسبوك يومًا ما:** التوكن بيموت → أعد الخطوات من (ج) بس — 10 دقايق وهترجع.

### هـ) مهم لانستجرام
- انستجرام **بيرفض النشر إلا بصورة من لينك عام** — الأداة بترفع البطاقة تلقائيًا على مستضيف مجاني (telegra.ph) وبتنشر. لو فشل الرفع، بوست انستجرام بيتتخطى من غير ما يعطل الباقي
- اللينك بتاع موقعك **مش بيتحط في بوست انستجرام** (مش clickable هناك أصلًا) — حطه في **البايو** بتاع الحساب: `wasiya-taupe.vercel.app`

---

## 6) تغيير مواعيد النشر

الجدولة الحالية (بتوقيت UTC — مصر بتزيد عليها ساعتين/تلاتة حسب الصيف):

| السطر في post.yml | معناه في مصر |
|---|---|
| `cron: "0 5 * * *"` | 7-8 صباحًا |
| `cron: "0 11 * * *"` | 1-2 ظهرًا |
| `cron: "0 17 * * *"` | 7-8 مساءً |

التعديل: افتح `.github/workflows/post.yml` → عدّل الساعات → Commit. الصيغة: `"0 الدقيقة الساعة * * *"`.

---

## 7) إضافة وتعديل البوستات

كل البوستات في أول ملف `run.mjs` في مصفوفة اسمها `TEMPLATES` — كل بوست سطرين:

```js
["العنوان القصير للبطاقة (بدون إيموجي)",
 "نص البوست الكامل..."],
```

- ضيف بوست جديد في آخر القايمة قبل `];` — بنفس الشكل
- عدّل أي نص براحتك
- **قاعدة مهمة:** السطر الأول هو اللي بيطلع على **البطاقة** — خليه قصير (أقل من 45 حرف) ومن غير إيموجي
- عدد الـ 46 الحالي مش قدس الأقداس — زوّد براحتك

---

## 8) نصائح أمان الحسابات الجديدة

- **سخّن الحسابات أول 2-3 أيام**: انشر فيها يدويًا شوية بوستات/تفاعل قبل ما تربطها بالأداة
- **متبعتش كل حاجة بنفس الدقيقة**: الأداة بتختلف تلقائيًا لأن كل منصة ليها وقتها في الجدولة
- انستجرام حساس للسبام في الحسابات الجديدة — عشان كده بوستاته من غير لينك، واللينك في البايو
- **متغيرش باسوورد فيسبوك** وانت ناسي إنه بيقتل التوكن (لو غيّرته: أعد توليد التوكن من SETUP قسم 5-ج)
- لو حسيت حساب بيتبهدل (مشاهدات بتقل جدًا): وقّفه مؤقتًا — Run workflow مش هتشتغل أوتوماتيك لمنصته لو مسحت الـ Secret بتاعها

---

## 9) حل المشاكل

| المشكلة | السبب المحتمل | الحل |
|---|---|---|
| مفيش بوستات خالص | الـ Workflow متعطل | Actions → شوف لو في رسالة خطأ حمراء → افتح الـ Run → شوف آخر سطور اللوج |
| "chat not found" تليجرام | البوت مش أدمن في القناة / اسم القناة غلط | راجع قسم 3-ب — لازم `@اسم_القناة` بالظبط |
| تليجرام 401 Unauthorized | التوكن غلط | راجع قسم 3-أ — اعمل توكن تاني من BotFather وحدّث الـ Secret |
| X: 401 / Forbidden | التوكن اتولد قبل "Read and write" | Regenerate Access Token من جديد بعد التأكد من الإعداد — قسم 4-ب خطوة 4 |
| X: 403 client-not-enrolled | مشكلة باقة/مشروع | تأكد إن الـ App جوه Project وإن الحساب مفعل المطورين صح |
| فيسبوك 190 (token expired) | التوكن مات (باسوورد اتغير) | أعد الخطوات 5-ج من الأول |
| انستجرام: "container" error | الحساب مش Business أو مش متربط بالصفحة | قسم 5-أ خطوة 2 — لازم Professional/Business + مربوط بالصفحة |
| انستجرام: الصورة مش بتظهر | فشل رفع telegra.ph | هتتخطى تلقائيًا — لو بيتكرر قول لي ونبدل المستضيف |
| البطاقة حروفها مربعات | خطوط العربي مش موجودة على الـ Runner | مش مفروض تحصل (خطوة الخطوط في الـ workflow) — لو حصلت ابعتلي |
| بوستات اتوقفت بعد 60 يوم هدوء | GitHub بيقفل الجدولة للريبو الخامل | عندنا محمية: كل نشر بيعمل Commit لسجل post-log.md فالريبو دايمًا "نشط" |

---

## 10) التكلفة وليه مفيش تيك توك

| الخدمة | التكلفة |
|---|---|
| GitHub Actions (ريبو عام) | **ببلاش — بلا حدود** |
| Telegram Bot API | **ببلاش** |
| X API (باقة Free) | **ببلاش** (~500 بوست/شهر — إحنا 90) |
| Facebook Graph API | **ببلاش** (صفحتك الخاصة) |
| Instagram Content Publishing | **ببلاش** (25 بوست/يوم — إحنا 3) |
| استضافة الصور (telegra.ph) | **ببلاش** |
| **الإجمالي** | **0 جنيه / للأبد** |

**ليه مفيش تيك توك؟** تيك توك ما بيديشش API نشر للناس العاديين — لازم موافقة يدوية منهم للشركات، وأي أداة "غير رسمية" بتوصل بحسابك بتتعرض للحظر الفوري. مش عايزين نضيع حسابك.

**منصات ممكن تضاف بعدين:** Threads (API بقى متاح — سهل نضيفه)، LinkedIn (محتاج مراجعة + تجديد توكن كل 60 يوم). قولّي لو حابب أي واحد فيهم.
